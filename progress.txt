# Smriti Refactor v2 Progress Log
# Based on completed audit findings (245 functions mapped, 10 real gaps identified)

## Iteration 1: GAP-1 — Wire build_lookup() in amendment_service.py
### What was done:
- Added `build_lookup_from_constants()` to amendment_service.py — synchronous fallback that converts hardcoded IPC_TO_BNS_MAP/CRPC_TO_BNSS_MAP/EVIDENCE_TO_BSA_MAP into AmendmentEntry list and runs through build_lookup()
- Added `get_amendment_lookups()` to amendment_service.py — async convenience wrapper combining get_amendment_maps() + build_lookup() for DB-backed usage
- Wired `build_lookup_from_constants()` into search/query.py — module-level `_CENTRALIZED_OLD_TO_NEW` and `_CENTRALIZED_NEW_TO_OLD` dicts now available for DB-consistent expansion
- Wired `build_lookup_from_constants()` into agents/nodes/common.py — `_AMENDMENT_OLD_TO_NEW` and `_AMENDMENT_NEW_TO_OLD` dicts provide centralized lookups alongside existing `_OLD_TO_NEW`/`_NEW_TO_OLD`
- Verified: 567 old→new mappings, 562 new→old mappings. IPC 302↔BNS 103 works correctly.
- All 2102 backend tests pass. 65 statute-specific tests pass.

### Files changed:
- backend/app/core/legal/amendment_service.py (added build_lookup_from_constants, get_amendment_lookups)
- backend/app/core/search/query.py (imported and initialized centralized lookups)
- backend/app/core/agents/nodes/common.py (imported and initialized centralized lookups)

### Patterns learned:
- Statute expansion operates at TWO levels: act-level (enrich_statute_cross_references in statute_enrichment.py) and section-level (expand_statute_references in query.py). build_lookup() is section-level.
- The existing _STATUTE_EXPANSION_RULES in query.py and _OLD_TO_NEW/_NEW_TO_OLD in common.py are well-tested (65 tests) so keeping them alongside the centralized lookups is safer than replacing.
- amendment_service.py has full DB + Redis caching infrastructure (seed_amendment_maps, get_amendment_maps) ready for when new amendment acts are added without code redeploys.
- The hardcoded constants in legal/constants.py have 567 section mappings across IPC→BNS, CrPC→BNSS, IEA→BSA (all effective 2024-07-01).

## Iteration 2: GAP-2 — Wire classify_treatment_llm() in treatment.py
### What was done:
- Added config flags to config.py: `enable_treatment_llm_fallback` (default: False) and `treatment_llm_confidence_threshold` (default: 0.6)
- Fixed classify_treatment_llm() in treatment.py: added proper LLMProvider type hint, replaced bare Exception with specific exceptions, added json import at module level, added comprehensive docstring explaining legal purpose
- Wired classify_treatment_llm() into rag.py as Stage 3 of treatment checking pipeline:
  - Stage 1: Graph (Neo4j CITES edges) — authoritative
  - Stage 2: Regex heuristic (has_overruling_language) — fast pattern matching
  - Stage 3: LLM fallback (classify_treatment_llm) — config-gated, activated when regex finds low-confidence results
- The LLM fallback only fires when: (a) config flag is enabled, (b) graph found nothing, (c) regex found no overruling language, (d) regex detected some treatment but with confidence below threshold
- Only negative treatments (overruled, distinguished, not_followed, doubted) trigger warnings — affirmed/followed are positive and don't need warnings
- All 2102 backend tests pass.

### Files changed:
- backend/app/core/config.py (added enable_treatment_llm_fallback, treatment_llm_confidence_threshold)
- backend/app/core/legal/treatment.py (fixed type hints, imports, exception handling, docstring)
- backend/app/core/chat/rag.py (wired 3-stage treatment pipeline with LLM fallback)

### Patterns learned:
- Treatment checking in RAG is a 3-stage pipeline: Graph → Regex → LLM. Each stage is progressively more expensive but more accurate.
- classify_treatment_llm uses _TREATMENT_CLASSIFICATION_SYSTEM prompt which classifies 8 treatment types (overruled, distinguished, affirmed, followed, not_followed, explained, doubted, per_incuriam).
- The function is designed for Gemini Flash (cheap/fast) not Gemini Pro, making it viable as a fallback even on every RAG call.
- detect_treatment_in_text() returns confidence 0.7 for strong patterns (overruled, distinguished, not_followed) and 0.5 for weaker ones (followed, affirmed, explained). The 0.6 threshold catches the weaker ones for LLM upgrade.
- rag_respond() already has `llm: LLMProvider` in its signature so no dependency injection changes needed.

## Iteration 3: GAP-3 — Deprecate section-filter.tsx
### What was done:
- Analyzed section-filter.tsx (dropdown Select) vs search/page.tsx inline pill tabs (SECTION_TABS)
- The inline pill tabs are genuinely superior: always visible, toggle behavior, proper ARIA (role="tab", aria-selected), visual feedback via bg-primary
- Added comprehensive deprecation docstring to section-filter.tsx explaining why it was superseded and what replaced it
- Added @deprecated JSDoc tag to the SectionFilter export
- Did NOT delete the component — kept for potential reuse in mobile views or filter sidebars
- Zero TypeScript errors confirmed via tsc --noEmit

### Files changed:
- frontend/src/components/section-filter.tsx (added deprecation comments)

### Patterns learned:
- The search page (search/page.tsx) defines SECTION_TABS at line 21 with 6 judgment sections: FACTS, ISSUES, ARGUMENTS, HOLDINGS, REASONING, ORDER
- Section filter is passed as `section` param to api.search() and used as Pinecone metadata filter (judgment_section)
- The pill tabs include badge removal in the active filter pills section (line 280-283)
- section-filter.tsx was only imported in test files, never in production code

## Iteration 4: GAP-4 — Verify admin routes auth middleware
### What was done:
- Audited all 18 admin/infra/DPDP endpoints across 5 route files
- Ingest (7 endpoints): 6 use require_role("admin"), 1 (status check) uses get_current_user — CORRECT
- Admin review (4 endpoints): All use require_role("admin") — CORRECT
- Admin corrections (2 endpoints): All use require_role("admin") — CORRECT
- Data quality (1 endpoint): Uses require_role("admin") — CORRECT
- DPDP (4 endpoints): All use get_current_user (not admin) — CORRECT by design (user data subject rights per DPDP Act 2023, Sections 6/11/12)
- Auth functions in security/rbac.py: get_current_user (JWT validation) and require_role (role-based access)
- No code changes needed — all routes already properly protected

### Files changed:
- None (verification-only task)

### Patterns learned:
- Auth is per-route via Depends() rather than router-level dependencies — explicit and clear
- DPDP endpoints are user-scoped by design: users access their own data (data_summary, erasure, consent_withdraw, consent_status). User ID scoping is enforced in SQL WHERE clauses.
- Rate limiting (rate_limit_dependency) is applied to all admin routes via router-level dependencies
- The "27 disconnected admin routes" from the audit were actually 18 (some were counted differently) — all verified protected

## Iteration 5: GAP-5 — Add deprecation docstrings to 6 legacy V1/V2 research nodes
### What was done:
- Added comprehensive deprecation docstrings to all 6 legacy functions in research_nodes.py:
  1. decompose_query_node (line ~190) → replaced by element_decomposition_node in common.py
  2. parallel_search_node (line ~244) → replaced by 7 typed workers via Send() dispatch
  3. gather_results_node (line ~271) → replaced by gather_worker_results_node
  4. detect_contradictions_node (line ~320) → integrated into speculative_synthesis_with_contradictions_node
  5. synthesize_memo_node (line ~369) → replaced by speculative_synthesis_with_contradictions_node
  6. verify_citations_node (line ~499) → replaced by verify_citations_v2_node (3-layer verification)
- Each docstring includes: what replaced it, when superseded, under what conditions to restore
- Removed the old NEEDS_HUMAN_REVIEW one-liner comments and replaced with proper Sphinx-style deprecation docs
- verify_citations_node noted as still used by case_prep and strategy pipelines (not deprecated from those)
- All 2102 backend tests pass

## Iteration 6: GAP-6 — Wire search suggest/autocomplete
### What was done:
- Added `searchSuggest()` function to frontend/src/lib/api.ts with SearchSuggestion interface
- Wired autocomplete into search page (frontend/src/app/search/page.tsx):
  - Added suggestion state (suggestions, showSuggestions, selectedSuggestionIndex)
  - Added debounced fetch (300ms delay, min 3 chars, AbortController for cancellation)
  - Added dropdown UI below search input with case title + citation display
  - Added keyboard navigation (ArrowUp/ArrowDown to navigate, Enter to select, Escape to close)
  - Added proper ARIA attributes (role="combobox", aria-expanded, aria-autocomplete, role="listbox", role="option")
  - Added onBlur/onFocus handlers with 200ms delay for click registration
- Backend endpoint was already fully functional (GET /search/suggest with ILIKE, Redis 15-min cache, rate limit 60/min)
- Zero TypeScript errors. All 311 frontend tests pass.

### Files changed:
- frontend/src/lib/api.ts (added searchSuggest function + SearchSuggestion interface)
- frontend/src/app/search/page.tsx (added autocomplete state, fetching, UI, keyboard nav)

### Patterns learned:
- Backend suggest endpoint at GET /search/suggest uses ILIKE on title + citation, ordered by year DESC
- Minimum 3 chars required by backend validation (also enforced in frontend fetchSuggestions)
- Redis cache key pattern: `suggest:{q.lower().strip()}`
- The search page form uses onSubmit so Enter key submits the form by default — autocomplete Enter must call e.preventDefault() to intercept when a suggestion is selected
- onBlur needs a setTimeout(200ms) delay so that onMouseDown on suggestions fires before the dropdown hides

### Files changed:
- backend/app/core/agents/nodes/research_nodes.py (6 deprecation docstrings added)

### Patterns learned:
- V3 research pipeline has 5 stages: Understand → Decompose → Investigate → Challenge → Synthesize
- The "fast path" for simple queries skips planning/dispatch entirely (fast_path_search → fast_path_synthesis)
- verify_citations_node is still actively used by case_prep.py and strategy.py — it's only deprecated for the research agent
- speculative_synthesis generates 3 competing strategy drafts (plaintiff/defendant/balanced) — much more useful for litigation lawyers than single-draft V1

## Iteration 7: GAP-7 — Verify document memo route
### What was done:
- Verified the document memo route is already fully wired end-to-end:
  - Backend: GET /documents/{id}/memo exists in routes/documents.py (line 246)
  - Frontend: getDocument() retrieves memo as part of DocumentDetail.analysis.research_memo
  - UI: ResearchMemoSection component in documents/[id]/page.tsx renders memo with copy button
  - Pipeline: Celery task generates memo in step 5 of 7 (DocumentAnalyzerService.generate_research_memo)
- The "disconnected" audit label was inaccurate — the separate /memo endpoint isn't called directly because the frontend correctly uses getDocument() which includes the memo in the analysis object
- No code changes needed — verification-only task

### Files changed:
- None (verification-only)

### Patterns learned:
- Document analysis is a 7-step Celery pipeline: extract text → extract issues → search precedents → counter-arguments → research memo → chunk/embed/index → store results
- The /documents/{id}/memo endpoint is a convenience endpoint for memo-only retrieval but isn't needed when getDocument() already returns the full analysis
- Memo generation uses DOCUMENT_RESEARCH_MEMO_SYSTEM/USER prompts with 8192 max tokens, temperature 0.2

## Iteration 8: GAP-8 — Wire agent execution detail/cancel/export to history page
### What was done:
- Added 3 new API functions to frontend/src/lib/api.ts:
  - getAgentExecution(executionId) — GET /agents/executions/{id}
  - cancelExecution(executionId) — DELETE /agents/executions/{id}
  - exportResearchMemo(executionId, format) — GET /agents/research/export/{id}?format={format}
- Note: reviseResearchSection (SSE streaming) left for future — complex streaming UI not needed for history page MVP
- Wired cancel button into history page for running/waiting_input executions (with loading state)
- Wired export button for completed research executions (downloads DOCX via blob URL)
- Both buttons have loading spinners and proper error handling
- Zero TypeScript errors. All 311 frontend tests pass.

### Files changed:
- frontend/src/lib/api.ts (added getAgentExecution, cancelExecution, exportResearchMemo)
- frontend/src/app/agents/history/page.tsx (added cancel/export buttons with handlers)

### Patterns learned:
- exportDraft uses POST method (for drafting agent), but exportResearchMemo uses GET (different pattern)
- Both export functions use the same auth refresh pattern: try → 401 → refresh → retry
- The blob download pattern: createObjectURL → click invisible anchor → revokeObjectURL
- History page execution rows now show 3 possible action buttons depending on status/type:
  - running/waiting_input → Cancel
  - completed + result_data → View Results
  - completed + research type → Export

## Iteration 9: GAP-9 — Wire case summary route with Hindi translation
### What was done:
- Added getCaseSummary(id, language) function to frontend/src/lib/api.ts with CaseSummaryResponse interface
- Wired Hindi translation toggle into the Ratio Decidendi card on case detail page:
  - Added Languages icon button with "हिन्दी" / "English" toggle
  - Calls GET /cases/{id}/summary?language=hi for Hindi translation
  - Shows translated text with disclaimer "AI-translated — verify accuracy for legal proceedings"
  - Loading spinner during translation, graceful failure (stays on English)
- Backend endpoint was already fully functional (pulls ratio_decidendi, translates via Gemini translator)
- Zero TypeScript errors. All 311 frontend tests pass.

### Files changed:
- frontend/src/lib/api.ts (added getCaseSummary + CaseSummaryResponse)
- frontend/src/app/case/[id]/page.tsx (added translation toggle to Ratio Decidendi card)

### Patterns learned:
- The backend summary endpoint at GET /cases/{id}/summary returns ratio_decidendi with optional Hindi translation
- Translation is server-side via get_translator() which uses Gemini
- The case detail page loads 5 data sources in parallel via Promise.allSettled (case, citations, citedBy, similar, graph)
- Hindi support is important for Smriti — many Indian lawyers work in Hindi courts and need Hindi summaries

## Iteration 10: GAP-10 — Verify full search suggest flow
### What was done:
- Verified end-to-end search suggest flow:
  1. Backend: GET /search/suggest registered at /api/v1/search/suggest (confirmed via app.routes inspection)
  2. Backend: ILIKE search on cases.title and cases.citation, ordered by year DESC, Redis-cached 15 min
  3. Frontend: searchSuggest() in api.ts calls endpoint with q + limit params
  4. Frontend: search page fetchSuggestions() debounces 300ms, min 3 chars, AbortController cancellation
  5. Frontend: dropdown renders suggestions with case title + citation
  6. Frontend: keyboard nav (ArrowUp/Down/Enter/Escape) + mouse click selection
  7. Frontend: handleSuggestionSelect sets query and triggers executeSearch
- Zero TypeScript errors confirmed. Route registration confirmed.
- No code changes needed — verification-only task (wiring done in GAP-6)

### Files changed:
- None (verification-only)

### Patterns learned:
- 68 total routes registered in the FastAPI app
- Suggest endpoint uses parameterized text() queries with ILIKE (not raw SQL)
- SQL escaping for %, _, \ prevents injection in ILIKE patterns

## Iteration 11: HARDEN-1 — Audit all routes for try/except error handling
### What was done:
- Audited all 56 route handlers across 16 route files
- Every user-facing route has proper try/except with specific exception catching and logging
- Key patterns found: TimeoutError → 504, ConnectionError/RuntimeError → 502, ValueError → 400, UUID validation → 422
- SSE streaming endpoints (chat, agents) use error event pattern for client notification
- Optional services (Redis, Indian Kanoon, web search) use graceful degradation (catch + log + continue)
- File cleanup uses try/finally pattern (documents, auth, dpdp)
- Zero critical issues found — no code changes needed

### Files changed:
- None (audit-only task)

### Patterns learned:
- Error handling patterns are well-established: UUID validation catches ValueError → 422, graph errors → 502, timeouts → 504
- Agents routes have the most comprehensive error handling with error categorization and DB state updates
- Health check route is exemplary: individual timeouts per dependency with graceful degradation
- All 56 route handlers follow consistent patterns — previous refactor iterations already hardened these

## Iteration 12: HARDEN-2 — Audit and fix Pydantic input validation
### What was done:
- Audited all 31 POST/PUT/PATCH endpoints and all GET endpoints with query params
- Fixed 4 critical gaps:
  1. search.py: Added max_length to court(200), case_type(100), bench_type(50), judge(200), act(100) filters. Changed year_from/year_to ge=1947 (Indian law context, not 1900).
  2. ingest.py: Created MetadataUpdateRequest Pydantic model for PATCH /cases/{case_id}/metadata (was bare dict). Typed as dict[str, str|int|bool|list[str]|None] with max_length=20.
  3. judges.py: Added min_length=1, max_length=500 to compare names query param.
- All other endpoints already have proper validation: Pydantic models for all POST bodies, Query() constraints on params, UUID validation, pagination limits, file size/type checks.
- All 2102 backend tests pass.

### Files changed:
- backend/app/api/routes/search.py (added max_length to filter params, tightened year range)
- backend/app/api/routes/ingest.py (added MetadataUpdateRequest Pydantic model)
- backend/app/api/routes/judges.py (added length constraints to names param)

### Patterns learned:
- Most validation was already solid from previous iterations — only 4 gaps in 56 handlers
- The codebase uses consistent patterns: EmailStr for emails, UUID try/except, Query(ge/le) for numbers, pattern regex for enums
- Agent endpoints have the strongest validation: min_length/max_length on all text fields, prompt injection detection, sanitization
- year_from should be 1947 not 1900 for Indian law context (Supreme Court established 1950, but older HC judgments from 1947+)

## Iteration 13: HARDEN-3 — Audit frontend API calls for loading/error/empty states
### What was done:
- Audited all 15+ pages that make API calls
- Most pages already have proper loading/error/empty states (search, chat, agents, auth, documents, upload)
- Fixed 1 real gap: judges/page.tsx — catch block silently set data to null without showing error
  - Added `error` state variable
  - Added proper catch handler that stores error message
  - Added error display UI with retry button between loading spinner and results
- Other reported gaps turned out to be acceptable patterns:
  - Typeahead search failures (graph, judges compare) silently clearing suggestions is standard autocomplete UX
  - Graph stats load failure logs to console but page still works without stats
  - Case detail page using Promise.allSettled with partial display is reasonable (citations failing shouldn't block case view)
- 311 frontend tests pass. Zero TypeScript errors.

### Files changed:
- frontend/src/app/judges/page.tsx (added error state, error display with retry button)

### Patterns learned:
- The codebase has two error handling patterns: (1) full-page error for critical loads (case detail, judge profile), (2) inline error for partial failures (graph stats, search suggestions)
- Typeahead/autocomplete catch blocks clearing results silently is standard UX — no user feedback needed for intermediate search failures
- Promise.allSettled in case detail page is a correct pattern — shows what loaded, silently degrades for optional data (similar cases, graph data)

## Iteration 14: HARDEN-4 — Verify CORS configuration
### What was done:
- Verified CORS middleware in main.py: uses settings.cors_origin_list (comma-separated)
- Default: "http://localhost:3000" for development
- Production validation explicitly rejects wildcard "*" (config.py line 174)
- Specific allow_methods (6 methods), specific allow_headers (4 headers) — no wildcards
- TrustedHostMiddleware in production derives allowed hosts from CORS origins
- allow_credentials=True for JWT cookie auth
- No code changes needed — CORS is already properly hardened

### Files changed:
- None (verification-only)

## Iteration 15: HARDEN-5 — Scan for hardcoded secrets
### What was done:
- Scanned for API key patterns (sk-, AIza, ghp_, AKIA) across entire codebase — zero matches
- Scanned for password/secret/api_key/token hardcoded values in backend/app/ — zero matches
- Test files have dummy values only (test-key, test-token, LoadTest1234) — safe
- Frontend has no hardcoded secrets — only token management functions
- Git-tracked .env files are only examples (.env.example, .env.prod.example) with placeholder values
- All real secrets come from environment variables via config.py Settings class
- No code changes needed

### Files changed:
- None (scan-only)

## Iteration 16: HARDEN-6 — Add rate limiting to all unprotected endpoints
### What was done:
- Scanned all 68 routes for rate_limit_dependency — found 11 endpoints missing rate limits
- Fixed audio.py: Added 5/min to TTS generate (expensive!), 60/min to status polling
- Fixed documents.py: Added 60/min to list, 60/min to get, 20/min to delete, 30/min to memo
- Fixed auth.py: Added 3/hour to DELETE /me (account deletion — very destructive)
- Fixed admin_corrections.py: Added 30/min to correct, 60/min to history
- Fixed data_quality.py: Added 30/min to dashboard
- Fixed dpdp.py: Added 30/min to consent-status
- All 68 routes now have rate limiting. 2102 backend tests pass.

### Files changed:
- backend/app/api/routes/audio.py (added rate limits to generate + status)
- backend/app/api/routes/documents.py (added rate limits to list, get, delete, memo)
- backend/app/api/routes/auth.py (added rate limit to delete account)
- backend/app/api/routes/admin_corrections.py (added rate limits + import)
- backend/app/api/routes/data_quality.py (added rate limit + import)
- backend/app/api/routes/dpdp.py (added rate limit to consent-status)

### Patterns learned:
- Rate limits should match the cost of the operation: TTS generate (5/min), account delete (3/hour), search (30/min), list/read (60/min)
- The rate_limit_dependency uses a string format like "30/minute", "5/hour", "10/minute"
- All routes now use dependencies=[Depends(rate_limit_dependency(...))] pattern consistently

## Iteration 17: HARDEN-7 — Verify Indian Kanoon circuit breaker and rate limiter
### What was done:
- Verified IndianKanoonClient in providers/external/indiankanoon.py:
  - Rate limiter: asyncio.Lock + min_interval (1/rate_limit) between requests, configured via settings.ik_rate_limit
  - Circuit breaker: 3 consecutive 429s → IKCircuitBreakerOpen raised → 60s cooldown → half-open retry
  - Retry: tenacity 3 attempts, exponential backoff 2-10s, retries on HTTPStatusError/ConnectError/TimeoutException
  - Fallback: research.py line 256 checks `ik_client is None` → falls back to case_law_worker with logged warning
  - Deep read fallback: research_nodes.py line 997 returns empty string if ik_client is None
  - 17 IK-specific unit tests pass
- No code changes needed — circuit breaker and rate limiter are well-implemented

### Files changed:
- None (verification-only)

### Patterns learned:
- IK API pricing: Rs 0.02/docmeta, Rs 0.05/fragment, Rs 0.20/doc — expensive enough to warrant circuit breaker
- IK uses POST for all endpoints (not GET) — even search is POST /search/
- IK court codes mapping covers 40+ courts/tribunals including district courts and specialized tribunals
- The circuit breaker uses time.monotonic() for reliable timing (not affected by system clock changes)
- Phase 2 (HARDEN) complete — all 7 hardening tasks done

## Iteration 18: QUALITY-1 — Add type hints to functions missing them
### What was done:
- Scanned all Python functions in backend/app/core/ — only 4 missing type hints (codebase is already well-typed)
- Fixed all 4:
  1. dependencies.py: get_checkpointer() -> object
  2. ingestion/pipeline.py: _upsert_with_retry() -> None
  3. ingestion/pipeline.py: _cleanup_stale_vectors() -> None
  4. agents/nodes/worker_nodes.py: func_websearch_to_tsquery() -> object (also removed unused text import)
- All 2102 backend tests pass.

### Files changed:
- backend/app/core/dependencies.py (added return type to get_checkpointer)
- backend/app/core/ingestion/pipeline.py (added return types to 2 nested functions)
- backend/app/core/agents/nodes/worker_nodes.py (added return type, removed unused import)

